# ── Gradle / Android build ──────────────────────────────────────────
.gradle/
build/
**/build/
.kotlin/
*.hprof

# Machine-specific SDK path (copy from local.properties.example)
local.properties

# ── Android Studio / IDE ────────────────────────────────────────────
.idea/
!.idea/gradle.xml
!.idea/misc.xml
!.idea/runConfigurations/
*.iml
.navigation/
captures/
.externalNativeBuild/
.cxx/
obj/

# ── Built artifacts (use GitHub Releases for APKs, not the repo) ───
*.apk
*.ap_
*.aab
*.dex
*.idsig
release/

# ── Secrets & signing (never commit) ───────────────────────────────
app/google-services.json
google-services.json
*.keystore
*.jks
.env
.env.*

# ── OS junk ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / agent (local only) ─────────────────────────────────────
.cursor/
.vscode/

# ── Python cache (if any helper scripts locally) ────────────────────
__pycache__/
*.py[cod]

# ── Optional local-only notes (not for GitHub) ──────────────────────
FIREBASE_SETUP.md
CAMPUS_FOUND_IMAGE_GUIDE.md
PROJECT_TECHNOLOGY_SUMMARY.txt

# Local automation scripts (README documents them; keep machine-specific)
scripts/
script/

# UI design drafts (not the Android app)
stitch_campus_lost_found/

# Dev / setup extras (keep locally — not needed for teacher submission)
docs/
postman/
firebase/
